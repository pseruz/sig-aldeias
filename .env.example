# Database — desenvolvimento local (recomendado)
DATABASE_URL=sqlite:///./data/sig_aldeias.db
# Produção / PostGIS (opcional, só se o servidor PostgreSQL estiver ativo)
# DATABASE_URL=postgresql://sig_user:sig_pass@localhost:5432/sig_aldeias

# Security
SECRET_KEY=your-secret-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

#Application
DEBUG=True
HOST=0.0.0.0
PORT=8000